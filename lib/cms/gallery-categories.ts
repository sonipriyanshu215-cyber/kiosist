// Shared between the public Culture page's filter tabs and the admin
// upload form, so a photo tagged in /admin/media always has a matching tab
// to show up under on the site.
export const GALLERY_CATEGORIES = [
  "Office space",
  "Training Session",
  "Team",
  "Festival Celebration",
  "Fun Friday",
  "Office Outings",
] as const;
